class_name: WebGuy
modules:
    enabled:
        - WebDriver

    config:
        WebDriver:
            browser: chrome
            window_size: maximize
            url:

env:
    test:
        modules:
            config:
                WebDriver:
                    env: test
                    host: 'localhost'
                    port: '4444'
                    browser: 'chrome'
                    window_size: maximize

